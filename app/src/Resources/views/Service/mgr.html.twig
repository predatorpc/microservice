<!DOCTYPE html>
<html>
<head>
    <title>Welcome to Symfony!</title>
</head>
<body>
<h1>{{ page_title }}</h1>

<ul id="navigation">
    {% for item in data %}
        <li><a href="{{ item.source }}">{{ item.timestamp }}</a></li>
    {% endfor %}
</ul>
</body>
</html>